[
  {
    "id": "demo-sandbox",
    "claim": "The demo uses separate sample data and can be reset.",
    "where": "demo banner and README",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context at /demo; edit, inspect storage keys, and reset"
  },
  {
    "id": "five-tasks",
    "claim": "The free audit supports five critical tasks.",
    "where": "landing facts and README",
    "test": "npm run test:e2e -- --grep @claim:five-tasks",
    "sandbox": "fresh browser context with the five-task demo"
  },
  {
    "id": "license-unlock",
    "claim": "A valid one-time license creates a private report link that opens without an account.",
    "where": "landing paid tier, report sharing section, and README",
    "test": "npm run test:e2e -- --grep @claim:license-unlock",
    "sandbox": "fresh creator context with a documented valid billing response and mocked report endpoint; open the created link in a clean browser context"
  },
  {
    "id": "html-export",
    "claim": "The audit exports an accessible standalone HTML report.",
    "where": "landing, report toolbar, and README",
    "test": "npm run test:e2e -- --grep @claim:html-export",
    "sandbox": "demo report; download and inspect the HTML file"
  },
  {
    "id": "json-export",
    "claim": "The audit exports its five tasks as JSON.",
    "where": "report toolbar and README",
    "test": "npm run test:e2e -- --grep @claim:json-export",
    "sandbox": "demo report; download JSON and assert schema, product, and five tasks"
  },
  {
    "id": "anonymous-export",
    "claim": "An anonymized JSON export removes product and environment names.",
    "where": "report toolbar and README",
    "test": "npm run test:e2e -- --grep @claim:anonymous-export",
    "sandbox": "demo report; enable anonymization, download JSON, and assert withheld fields and absent source names"
  },
  {
    "id": "free-local-storage",
    "claim": "Free audits stay in browser storage until a user chooses to create a private link.",
    "where": "landing facts, privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:free-local-storage",
    "sandbox": "fresh real-audit context; create and edit an audit, assert local storage and no API or off-origin request"
  },
  {
    "id": "hosted-checkout",
    "claim": "Team sharing costs $39 once and uses Sociobot and Dodo for checkout and refunds.",
    "where": "landing paid tier, privacy page, terms page, and README",
    "test": "npm run test:e2e -- --grep @claim:hosted-checkout",
    "sandbox": "landing page; assert displayed price, exact Sociobot hosted checkout URL, and payment/refund disclosure"
  },
  {
    "id": "offline-reload",
    "claim": "The demo works offline after the first visit.",
    "where": "landing facts and README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load demo, wait for service worker, go offline, and reload"
  },
  {
    "id": "shared-expiry",
    "claim": "Private report links expire after 30 days.",
    "where": "landing paid tier, shared report page, and README",
    "test": "cargo test claim_shared_links_expire_after_30_days",
    "sandbox": "in-memory SQLite database; create a report, assert its expiry timestamp, then retrieve it"
  }
]
